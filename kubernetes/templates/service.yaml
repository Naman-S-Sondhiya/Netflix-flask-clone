apiVersion: v1
kind: Service
metadata:
  name: netflix-app
  namespace: {{ .Values.namespace }}
  labels:
    app: netflix-app
spec:
  selector:
    app: netflix-app
  type: NodePort
  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 30007
